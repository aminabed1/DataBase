spring:
  application:
    name: GangOfThree
  
  datasource:
    url: jdbc:postgresql://localhost:5432/football_ticket_db
    username: postgres
    password: aminamin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false

management:
  health:
    redis:
      enabled: false

server:
  port: 8081