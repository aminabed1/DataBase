spring:
  application:
    name: GangOfThree

  elasticsearch:
    uris: ${SPRING_ELASTICSEARCH_URIS:http://localhost:9200}

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:aminamin}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false

  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}

  mail:
    host: smtp.gmail.com
    port: 587
    username: amin.m.abdollahi@gmail.com
    password: "bvih uium nnbx kpjb"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          debug: true

jwt:
  secret: LFWCaUBxcUh6QhMktoYfJdCymfsiLaze4Gbcqpj4j4EIjpSCISoHfQ/iG+rF43qEfT731G4roqFWc6kEPmKOOg==
  access-expiration: 10h
  reset-expiration: 15m
  register-expiration: 30m

server:
  port: ${SERVER_PORT:8081}